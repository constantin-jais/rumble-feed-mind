# FeedMind - BMAD Configuration
# BMad Method (BMM) config for PRD workflow

project_name: FeedMind
version: "0.1.0"
user_name: "[À configurer]"
date: "2026-01-27"

# Languages
communication_language: fr
document_output_language: fr

# Output paths
output_folder: _bmad/docs
planning_artifacts: _bmad/docs

# User context
user_skill_level: expert
project_context: greenfield

# Project classification (from BMAD discovery)
classification:
  project_type: saas_platform
  domain: content_aggregation
  complexity: high

# Features flags for PRD
features:
  ai_integration: true
  multi_tenant: true
  self_hosted: true

# Stakeholders
stakeholders:
  - role: founder
    name: "[À configurer]"
    focus: "Product vision, technical architecture"
